// Este archivo puede manejar cualquier interacción futura con la web.
// Actualmente solo gestiona la visualización de los modales en la página.
document.addEventListener('DOMContentLoaded', function () {
    var modalEl = document.getElementById('historiaModal');
    var myModal = new bootstrap.Modal(modalEl);
});
